---
phase: 02-notion-sync-audit-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/fetch-notion.mjs
  - src/content/notes/**/*.md
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-06
  - SYNC-07
  - SYNC-08
user_setup:
  - service: notion
    why: "Notion API access to fetch content"
    env_vars:
      - name: NOTION_TOKEN
        source: "Notion integrations page (https://www.notion.so/my-integrations) -> your integration -> Internal Integration Secret"

must_haves:
  truths:
    - "Script runs without errors and generates .md files for all courses/weeks/lessons"
    - "No separate subsection files exist — only section-level files"
    - "All images in generated markdown reference existing files in public/images/"
    - "Frontmatter in every file matches content.config.ts Zod schema"
    - "Order values are sequential integers (no compound 101/201 values)"
  artifacts:
    - path: "src/content/notes/"
      provides: "Generated markdown files for all courses"
    - path: "public/images/"
      provides: "Downloaded images referenced by markdown"
  key_links:
    - from: "src/content/notes/**/*.md"
      to: "src/content.config.ts"
      via: "frontmatter fields match Zod schema"
      pattern: "title.*course.*courseSlug.*courseOrder.*week.*weekSlug.*weekOrder.*order"
    - from: "src/content/notes/**/*.md"
      to: "public/images/"
      via: "image references point to existing files"
      pattern: "!\\[.*\\]\\(/images/"
---

<objective>
Run the fixed fetch-notion.mjs script, then validate that all output is correct: files generated, images referenced, frontmatter valid, hierarchy flattened.

Purpose: Confirm the script fixes from Plan 01 produce correct output against the real Notion API.
Output: Validated markdown content in src/content/notes/ ready for subsequent phases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-notion-sync-audit-fix/02-CONTEXT.md
@.planning/phases/02-notion-sync-audit-fix/02-01-SUMMARY.md
@src/content.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run fetch script and validate output</name>
  <files>src/content/notes/**/*.md, public/images/**</files>
  <read_first>
    - scripts/fetch-notion.mjs
    - src/content.config.ts
    - .planning/phases/02-notion-sync-audit-fix/02-CONTEXT.md (D-01 through D-04 for expected structure)
  </read_first>
  <action>
1. **Run the script:** `NOTION_TOKEN=$NOTION_TOKEN node scripts/fetch-notion.mjs`
   - If NOTION_TOKEN is not set, this task will create a checkpoint for the user to provide it.
   - Script should complete without errors.

2. **Validate file structure:** Check that the output matches the flattened hierarchy per D-01:
   - `find src/content/notes -name "*.md" | wc -l` — count files generated
   - `find src/content/notes -name "*.md" | head -20` — verify directory structure is Course/Week/Section.md
   - No subsection-level files should exist (files should be at section level, not deeper)

3. **Validate frontmatter in every file:**
   - For each .md file, check that frontmatter contains: title, course, courseSlug, courseOrder, week, weekSlug, weekOrder, order
   - Verify `order` values are sequential integers (1, 2, 3...) not compound (101, 201...)
   - Verify `notionId` is present (optional in schema but expected)

4. **Validate image references:**
   - Extract all image references from markdown: `grep -rh '!\[' src/content/notes/ | grep -o '/images/[^)]*'`
   - For each referenced image path, verify the file exists in `public/images/`
   - Report any broken image references

5. **Validate block types present:** Spot-check that output contains:
   - Code blocks with language tags (` ```python `, ` ```sql `, etc.)
   - Tables with separator rows
   - Images inline in content
   - Headings (## for merged subsection titles)

6. **If any validation fails:** Fix the issue in `scripts/fetch-notion.mjs` and re-run. Common issues:
   - If order values are still compound: check the else branch was correctly replaced
   - If subsection files still exist: check the merge logic
   - If images are missing: check downloadImage is being called

7. **Run build to verify content schema:** `npm run build` should succeed, confirming all frontmatter matches the Zod schema.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (all frontmatter matches Zod schema)
    - `find src/content/notes -name "*.md" | wc -l` shows files generated (fewer than 52 due to flattening)
    - `grep -r "order:" src/content/notes/ | grep -E "order: [0-9]{3}"` returns NO matches (no compound order values like 101, 201)
    - No broken image references (all /images/*.png paths resolve to existing files)
  </acceptance_criteria>
  <done>Script runs successfully; all markdown files have valid frontmatter; images reference existing files; hierarchy is flattened</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify content output</name>
  <what-built>Fixed Notion sync script that flattens subsections and produces correct markdown with valid frontmatter, image references, and sequential ordering.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the site in browser
    2. Navigate through courses — verify lessons appear in correct order
    3. Check that previously-separate subsections now appear as sections within a single page (visible as ## headings)
    4. Verify images display correctly inline
    5. Spot-check a few pages for reasonable content structure
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- All generated .md files have frontmatter matching content.config.ts schema
- No compound order values (101, 201, etc.) in any frontmatter
- All image references point to existing files in public/images/
- Fewer files than before (subsections merged into sections)
</verification>

<success_criteria>
- fetch-notion.mjs runs without errors against Notion API
- Generated markdown files have correct structure: Course/Week/Section.md
- All frontmatter fields present and valid per Zod schema
- All image references resolve to existing files
- npm run build succeeds
- User confirms content looks correct in browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-notion-sync-audit-fix/02-02-SUMMARY.md`
</output>
