---
phase: 01-foundation-tooling
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - TOOL-03

must_haves:
  truths:
    - "`npm run build` completes successfully from the current working directory"
    - "`npm run lint` exits with code 0 and reports zero errors"
    - "`npm run typecheck` exits with code 0 and reports zero errors"
    - "All three phase success criteria are simultaneously true in a single shell session"
  artifacts:
    - path: "dist/"
      provides: "Built static site output (proves build passes)"
  key_links:
    - from: "package.json scripts"
      to: "build, lint, typecheck"
      via: "npm run"
      pattern: '"build"|"lint"|"typecheck"'
---

<objective>
Run the full verification gauntlet: lint, typecheck, and build all pass with zero errors. This plan produces no file changes — it validates that plans 01-01 and 01-02 collectively satisfy the phase success criteria.

Purpose: Confirm all three phase requirements (TOOL-01, TOOL-02, TOOL-03) are met before closing Phase 1.
Output: Passing command outputs and a human-verified checkpoint confirming the clean state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-tooling/01-01-SUMMARY.md
@.planning/phases/01-foundation-tooling/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite and capture output</name>
  <files></files>
  <read_first>
    - /Users/guyardon/Repositories/data-engineering-specialization/package.json
  </read_first>
  <action>
Run all three verification commands in sequence and capture their results:

```bash
npm run lint
echo "LINT_EXIT:$?"

npm run typecheck
echo "TYPECHECK_EXIT:$?"

npm run build
echo "BUILD_EXIT:$?"
```

All three must exit 0. If any fails:

- lint failure → go back to plan 01-01 gaps, fix errors, rerun
- typecheck failure → go back to plan 01-02 gaps, fix errors, rerun
- build failure → diagnose the Astro build error from the output, fix the specific file, rerun

Do not mark this task done until all three commands show EXIT:0.
</action>
<verify>
<automated>npm run lint 2>&1 && echo "LINT:OK" && npm run typecheck 2>&1 && echo "TYPECHECK:OK" && npm run build 2>&1 | tail -5 && echo "BUILD:OK"</automated>
</verify>
<acceptance_criteria> - `npm run lint` output contains "LINT:OK" (exit 0) - `npm run typecheck` output contains "TYPECHECK:OK" (exit 0) - `npm run build` output contains "Build Complete" or "Completed" and "BUILD:OK" (exit 0) - No output line from any command contains " error " at error severity - `ls dist/index.html` returns the file (build output exists)
</acceptance_criteria>
<done>All three commands exit 0. Build output exists at dist/. Phase success criteria 1, 2, and 3 confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 tooling stack:
    - ESLint + Prettier configured (plan 01-01)
    - TypeScript strict mode errors fixed (plan 01-02)
    - All three verification commands passing (task 1 above)
  </what-built>
  <how-to-verify>
    In your terminal, from the project root, run each command and confirm the result:

    1. `npm run lint`
       Expected: exits 0, no "error" output (warnings OK)

    2. `npm run typecheck`
       Expected: exits 0, output includes "0 errors" or similar

    3. `npm run build`
       Expected: exits 0, output ends with "Build Complete" or "Completed in Xms"

    4. Optional sanity check — `npm run preview` then visit http://localhost:4321
       Expected: site loads and sidebar shows course navigation

  </how-to-verify>
  <resume-signal>Type "approved" if all three commands pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when the human approves the checkpoint above. All three of the following must be simultaneously true:

1. `npm run lint` exits 0 — satisfies TOOL-01
2. `npm run typecheck` exits 0 — satisfies TOOL-02
3. `npm run build` exits 0 — satisfies TOOL-03
   </verification>

<success_criteria>

- Human has confirmed all three commands pass in their terminal
- No error-level output from lint, typecheck, or build
- Phase 1 can be marked complete in ROADMAP.md and STATE.md
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tooling/01-03-SUMMARY.md`

Also update:

- `.planning/ROADMAP.md` — mark Phase 1 plans as [x] complete
- `.planning/STATE.md` — update Current Phase status to "Complete", Next action to "Run /gsd:discuss-phase 2"
  </output>
