---
phase: 01-foundation-tooling
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/Sidebar.astro
  - src/content.config.ts
autonomous: true
requirements:
  - TOOL-02

must_haves:
  truths:
    - "`npm run typecheck` (astro check) exits with code 0 and reports 0 errors"
    - "Sidebar.astro uses `note.slug` consistently (the correct property on NoteItem)"
    - "Sidebar.astro uses `note.id` from Astro content collection entries (not NoteItem) to find currentNote"
    - "content.config.ts imports `z` from `zod` directly instead of `astro:content` to avoid deprecation"
  artifacts:
    - path: "src/components/Sidebar.astro"
      provides: "Fixed TypeScript errors — NoteItem uses slug not id; currentNote lookup uses n.id"
    - path: "src/content.config.ts"
      provides: "Uses zod directly instead of re-exported z from astro:content"
  key_links:
    - from: "src/components/Sidebar.astro"
      to: "NoteItem type"
      via: "note.slug property in map and isActive check"
      pattern: "note\\.slug"
---

<objective>
Fix the three TypeScript errors reported by `astro check` and resolve the deprecation warnings in content.config.ts. No UI or behaviour changes — only type correctness.

Purpose: Achieve zero errors from `npm run typecheck` (astro check) so TOOL-02 is satisfied.
Output: Two modified source files with correct types; typecheck passes clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-tooling/01-CONTEXT.md

<interfaces>
<!-- Errors found by `astro check` before this plan runs. -->
<!-- All three errors are in src/components/Sidebar.astro -->

Error 1 (line 55):
Property 'slug' does not exist on type CollectionEntry<"notes">
Expression: `notes.find(n => n.slug === currentSlug)`
Fix: Astro content collection entries expose `.id` not `.slug` in Astro v5+.
Change `n.slug` → `n.id`

Error 2 (line 90):
Property 'id' does not exist on type 'NoteItem'
Expression: `const isActive = note.id === currentSlug`
NoteItem is the local type defined in Sidebar.astro:
type NoteItem = { slug: string; title: string; order: number; }
Fix: NoteItem stores `slug` (set from note.id when building the tree).
Change `note.id` → `note.slug`

Error 3 (line 94):
Property 'id' does not exist on type 'NoteItem'
Expression: `href={`${BASE}/notes/${note.id}/`}`
Same NoteItem issue.
Fix: Change `note.id` → `note.slug`

Deprecation hints in src/content.config.ts (12 hints):
`z` imported from `astro:content` is deprecated in Astro v5.
Fix: Change `import { defineCollection, z } from 'astro:content'`
to `import { defineCollection } from 'astro:content'; import { z } from 'zod';`
Note: zod is already installed (now in devDependencies per plan 01-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix three TypeScript errors in Sidebar.astro</name>
  <files>src/components/Sidebar.astro</files>
  <read_first>
    - /Users/guyardon/Repositories/data-engineering-specialization-website/src/components/Sidebar.astro
  </read_first>
  <action>
Make exactly three targeted changes in src/components/Sidebar.astro:

**Change 1** — Line ~55 (currentNote lookup):
Before: `const currentNote = notes.find(n => n.slug === currentSlug);`
After: `const currentNote = notes.find(n => n.id === currentSlug);`
Reason: Astro v5 content entries expose `.id`, not `.slug`.

**Change 2** — Line ~90 (isActive check inside week.notes.map):
Before: `const isActive = note.id === currentSlug;`
After: `const isActive = note.slug === currentSlug;`
Reason: `note` here is a `NoteItem` (local type), which has `.slug` not `.id`.

**Change 3** — Line ~94 (href in anchor tag):
Before: `href={`${BASE}/notes/${note.id}/`}`
After: `href={`${BASE}/notes/${note.slug}/`}`
Reason: Same `NoteItem` — use `.slug`.

Do not change any other code, styles, or logic.
</action>
<verify>
<automated>grep -n "n\.slug\|note\.id" /Users/guyardon/Repositories/data-engineering-specialization-website/src/components/Sidebar.astro || echo "CLEAN — no remaining n.slug or note.id patterns"</automated>
</verify>
<acceptance_criteria> - `grep "n\.slug" src/components/Sidebar.astro` returns no output (old incorrect pattern gone) - `grep "note\.id" src/components/Sidebar.astro` returns no output (old incorrect pattern gone) - `grep "n\.id" src/components/Sidebar.astro` returns the currentNote find line (correct pattern present) - `grep "note\.slug" src/components/Sidebar.astro` returns two lines (isActive and href)
</acceptance_criteria>
<done>All three TypeScript errors in Sidebar.astro eliminated. NoteItem properties and CollectionEntry properties used correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Fix z import deprecation in content.config.ts and run typecheck</name>
  <files>src/content.config.ts</files>
  <read_first>
    - /Users/guyardon/Repositories/data-engineering-specialization-website/src/content.config.ts
  </read_first>
  <action>
In src/content.config.ts, change the import on line 1:

Before:
`import { defineCollection, z } from 'astro:content';`

After:
`import { defineCollection } from 'astro:content';`
`import { z } from 'zod';`

No other changes to the file. The schema definitions remain identical.

After making the change, run the full typecheck to confirm zero errors:

```bash
npm run typecheck
```

Interpret the output:

- "0 errors" or exit code 0 → task complete
- Any remaining errors → diagnose and fix before marking done
  </action>
  <verify>
  <automated>npm run typecheck 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria> - `grep "from 'astro:content'" src/content.config.ts` returns only `import { defineCollection } from 'astro:content'` (no `z` on that line) - `grep "from 'zod'" src/content.config.ts` returns `import { z } from 'zod'` - `npm run typecheck` exits with code 0 - `npm run typecheck` output contains "0 errors" or no error-level lines - `npm run typecheck` output contains no lines matching `error ts\([0-9]+\)` at error severity
  </acceptance_criteria>
  <done>`npm run typecheck` (astro check) reports zero errors. All TypeScript strict mode requirements satisfied.</done>
  </task>

</tasks>

<verification>
After both tasks complete:

1. `npm run typecheck` — exits 0, output shows "0 errors"
2. `grep "n\.id\b" src/components/Sidebar.astro` — returns the find() line only
3. `grep "note\.slug" src/components/Sidebar.astro` — returns two lines (isActive + href)
4. `grep "from 'zod'" src/content.config.ts` — returns one line
5. `npm run build` — still succeeds (no regression)
   </verification>

<success_criteria>

- `npm run typecheck` exits 0 with zero errors
- Sidebar.astro correctly uses `n.id` for CollectionEntry lookup and `note.slug` for NoteItem properties
- content.config.ts imports z directly from 'zod' not from 'astro:content'
- `npm run build` still completes successfully (no regression introduced)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tooling/01-02-SUMMARY.md`
</output>
